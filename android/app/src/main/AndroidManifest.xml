<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:forceDarkAllowed="false"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Custom-scheme deep link for OAuth callbacks and invite URLs.
                 com.get-sessio.app://auth/callback (Google sign-in)
                 com.get-sessio.app://join/<code>  (training invite)
                 com.get-sessio.app://join-school/<code>  (school invite)
                 Capacitor forwards these to the JS appUrlOpen listener
                 (handled by src/lib/deep-links.ts). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.get-sessio.app" />
            </intent-filter>

            <!-- HTTPS App Links for https://get-sessio.com/* — verified via
                 /.well-known/assetlinks.json so Android opens the app instead
                 of the browser. autoVerify must be true on every <intent-filter>
                 element for the same host or verification fails. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="get-sessio.com" android:pathPrefix="/join-school/" />
                <data android:scheme="https" android:host="get-sessio.com" android:pathPrefix="/join/" />
                <data android:scheme="https" android:host="get-sessio.com" android:pathPrefix="/auth/callback" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
