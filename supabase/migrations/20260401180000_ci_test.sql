-- CI/CD test migration — adds a comment to verify auto-deploy works.
-- Safe to delete after confirming the GitHub Action runs successfully.
COMMENT ON SCHEMA public IS 'Sessio app schema — CI/CD verified';
